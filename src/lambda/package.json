{
  "name": "finos-qbo-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Token broker keeping QuickBooks credentials inside AWS",
  "scripts": {
    "deploy": "node deploy.mjs",
    "destroy": "node destroy.mjs",
    "urls": "node deploy.mjs --urls",
    "test:flow": "node test-flow.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-iam": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0",
    "@aws-sdk/client-ssm": "^3.700.0",
    "@aws-sdk/client-sts": "^3.700.0"
  },
  "engines": {
    "node": ">=20"
  }
}
